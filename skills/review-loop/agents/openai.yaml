interface:
  display_name: "Review Loop"
  short_description: "Bounded review-fix-review closeout"
  default_prompt: "Use $review-loop to sanity-check review findings, fix accepted issues, verify, and rerun review until clean or bounded."
policy:
  allow_implicit_invocation: true
