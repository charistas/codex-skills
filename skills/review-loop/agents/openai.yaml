interface:
  display_name: "Review Loop"
  short_description: "Bounded review-fix-review closeout"
  default_prompt: "Use $review-loop to review current local changes, fix verified findings, run appropriate checks, and stop after at most three review invocations."
policy:
  allow_implicit_invocation: true
